# M4 MISSION — DURABLE MEMORY / RESUME + INSTANCE LEASE

**Status:** READY-FOR-TOKEN (post research-swarm integration 2026-07-24)  
**Branch:** `feat/arsenal-wiring`  
**Canon:** codex-loop Round 19–20 scope amendments; IMPLEMENTATION-PLAN-A1 WS-1 + WS-4.4

## Why M4 is next (not swarm)

Swarm is a **research tool** with honest bounded runs (`RESEARCH_ONLY_LIMITED`).
Atlas becomes a **persistent agent** when memory survives process death, recall works,
and a second instance cannot silently corrupt state. That is M4 — the main module.

Swarm live diversity proof waits for **M6** (provider health + spend truth).

## Scope (frozen)

### A. Goal / exec-graph resume
- Kill-and-resume e2e: `atlas goal run` survives process death; persisted budgets intact (extends M3 R16 §2)
- Exec-graph task state durable across CLI restarts without manual repair

### B. Supabase write-back (no DDL in M4)
- Emotional memory / recall write-back hooks with regression tests
- Recall-correctness umbrella: family of bugs b2/b3 from IMPLEMENTATION-PLAN §7 (malformed filters, silent failures)
- Live Supabase DDL apply remains **CEO gate** — code + tests only until approved

### C. Instance anti-fork lease
- Instance-id + state lease (lockfile + heartbeat)
- Second concurrent launch → read-only with loud notice
- Crash / lease expiry tested (spawn-two test)
- Same safety family as M3 one-active-goal lease

### D. Write-back breadcrumb hook (organ 8, size S)
- Pre-exit / pre-compaction mechanical breadcrumb — exit without write-back blocked or warned loudly

## Explicit non-goals
- Self-compiling wiki (deferred from vague M4 wording)
- Swarm provider expansion or live proof
- Python VOLAURA swarm revival
- Key rotation, TRADER, deploy, Telegram product changes

## Definition of Done
- `npm run typecheck` + `npm test -- --run` green
- Kill-and-resume test: goal budgets survive simulated process death
- Spawn-two test: second instance read-only, no dual-writer corruption
- Recall regression tests for known failure family (filter malformed, write-back non-fatal logged)
- Adversarial review: memory integrity + lease safety
- ATLAS-STATE-NOW updated; codex-loop six-field entry

## Red lines
- No live Supabase DDL without CEO
- No second memory authority (VOLAURA product untouched)
- No LLM in deterministic resume/lease checks

## Entry command (executor, when token issued)
```
git checkout feat/arsenal-wiring && git pull --ff-only  # if remote exists
npm test -- --run
# implement M4 slices A→D with TDD; one commit per slice
```

## References
- `docs/atlas-cto/IMPLEMENTATION-PLAN-A1-2026-07-21.md` WS-1, WS-4.4, item 0.5
- `docs/adr/0003-append-only-ledger-plus-snapshot.md`
- codex-loop: M4 scope amendment (drop self-compiling recall floor)
