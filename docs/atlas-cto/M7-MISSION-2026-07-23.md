# M7 MISSION FILE — CONTROL-NOTIFY-SUPERVISED-ASSIST (token issue 2026-07-23)

**FABLE.GO:M7** — execution token for `terminal-atlas-executor`, single writer, WIP=1.

## Token header (amendments to the Round 23 brief — binding, do not weaken)

1. **CEO trigger received 2026-07-23 ~05:20 Baku**, verbatim: «дай задачу продолжить разработку атласа в клауд код. разбуди атласа пусть работает.» Issued through the Fable seat under direct CEO order; recorded in codex-loop Round 25.
2. **BASELINE RE-PIN.** The Round 23 brief pinned parent `0983154`. Superseded: baseline = the HEAD SHA passed to you in your spawn prompt (commit containing THIS file). Legitimacy receipt: `git diff 0983154..HEAD --name-only` = 6 files, ALL under `docs/` (ADR-0009, OPERATING-CANON, STATE-NOW, IMPLEMENTATION-PLAN-A1, SPEC-M8-EVIDENCE-AUDIT, SPEC-PORTABLE-LESSONS-LEDGER) — zero production/test files. Your first commands still prove: clean tree + HEAD == the SHA from your spawn prompt + `git diff 0983154..HEAD --name-only` shows only `docs/` paths. Any non-docs path in that diff → STOP without edits.
3. **TOOLCHAIN: NO UPDATE AUTHORIZED.** The CEO trigger did NOT include `claude update` approval. Local CLI is `2.1.109` (< 2.1.139). Per brief §2-3 fallback: do NOT run any install/update, do NOT invent native `/goal`; execute this entire brief as ONE bounded headless session and mark the native-loop item UNVERIFIED in your six-field report.
4. Everything else in the Round 23 brief below applies EXACTLY as written (scope, skills, architecture, file ownership, DoD, red lines, return contract). Where the brief says "0983154" as baseline, read it per amendment 2.

---

## Round 23 brief (codex-cto/verifier, verbatim)

```text
MISSION: M7 CONTROL-NOTIFY-SUPERVISED-ASSIST
CANON: Rounds 16, 19, 20, 22, 23 + ADR-0009+A1 + IMPLEMENTATION-PLAN-A1 v2 R-2/R-3
BASELINE: repo C:\Users\user\OneDrive\Documents\GitHub\ANUS; branch feat/arsenal-wiring; exact parent 0983154ca94554e89a91c06663b229ed2937b035; working tree must be clean.
[TOKEN AMENDMENT 2 APPLIES: baseline = spawn-prompt SHA; drift from 0983154 is docs-only, receipt above.]

1. EXECUTION BODY / MODEL
- Body: Terminal Atlas, Claude Code, Opus, SINGLE WRITER, WIP=1.
- Effort: high for the session. Use the literal instruction `ultrathink` ONCE before the architecture/threat-model decision; do not repeat it for mechanical edits.
- Topology: zero writer subagents and zero parallel worktrees. Fable is the independent per-gate verifier after return; Codex is final auditor. No second writer on control, notify, browser, goal-runner or CLI authority surfaces.

2. BATCHED TOOLCHAIN-UPDATE APPROVAL
[SUPERSEDED BY TOKEN AMENDMENT 3: no update authorized this run — fallback mode, native-loop UNVERIFIED.]

3. NATIVE LOOP / HARD BOUNDS
- Native /goal unavailable at 2.1.109 — run this brief as one bounded headless session.
- Goal condition: do not stop until every M7 acceptance item below is proven by commands in this session, or a hard stop fires.
- Hard stop: 180 minutes OR 120 agent/tool turns OR any red-line OR baseline drift OR three failed repair cycles on the same fingerprint. On stop: no false DONE, no weakened criteria, no partial green claim; append one honest six-field report with UNVERIFIED/BLOCKED items.

4. REQUIRED SKILLS
- skill-router
- writing-plans (make a short internal task map; do not create a new architecture document)
- executing-plans
- test-driven-development
- systematic-debugging
- verification-before-completion
- sentry-code-review
- sentry-commit
- using-git-worktrees (use it only for the anti-fork/baseline check; do not create a second writer worktree)
- webapp-testing (fixture-only browser E2E; no real portal)

5. PRODUCT OUTCOME
Ship one working M7 module with three joined capabilities:
A) one control truth for pause/resume across CLI, Telegram, autonomy and goal-runner;
B) one proactive/asynchronous CEO notification chokepoint with Baku quiet hours and panic exception;
C) foreground-only supervised browser assist that can fill only a CEO-approved answer pack verbatim, never submit and never mutate an external portal unexpectedly.

6. ARCHITECTURE / FILE OWNERSHIP

A. CONTROL — one durable state machine, hard overlays preserved
- Canonical state transition remains src/atlas/control-plane.ts. Route Telegram `/pause` and `/resume` through applyControlCommand(); remove the current process-env-only command fork in src/telegram.ts.
- `ATLAS_PAUSE` env and the local PAUSE file remain independent hard overlays. `/resume` may clear control-state pause but MUST NOT claim success while env/file pause is still active; response/status must name the active source.
- Goal runner, task spawner, autonomy tick, swarm/skill entry points and outbound notify re-check the same effective control decision immediately before each new action/send. Pause arriving mid-goal prevents the next Hand action; it never converts partial work to success.
- Expected existing files: src/atlas/control-plane.ts, src/atlas/spend-policy.ts, src/telegram.ts, src/cli.ts, src/goal-runner/runner.ts, src/atlas/autonomy-loop.ts, src/atlas/task-spawner.ts.
- Tests: extend control-plane.test.ts, spend-policy.test.ts, task-spawner-safety.test.ts, autonomy-loop.test.ts, goal-runner.test.ts. Prove CLI pause -> goal refuses -> resume -> goal can start using temp state only.

B. NOTIFY — one transport, typed quiet-hours queue
- Consolidate every proactive/asynchronous outbound Telegram send through src/atlas/notify.ts. Remove repo-watch.ts's private telegramSend/token/API path and telegram.ts direct `bot.telegram.sendMessage` asynchronous paths. Synchronous inbound `ctx.reply` may remain a reply path; it is not proactive authority.
- Only notify.ts may construct the Telegram send API request for proactive/asynchronous CEO sends. Caller cannot choose an arbitrary chat ID.
- Add coded quiet-hours policy: 23:00 inclusive to 08:00 exclusive in Asia/Baku. Non-panic allowed kinds during quiet hours return typed `QUEUED`, persist one durable redacted queue entry and do not call transport. `panic` bypasses quiet hours. Chatter remains SUPPRESSED, not queued.
- Add an explicit queue drain outside quiet hours: one compact CEO digest, atomic retain-on-failure / clear-on-success, no duplicate replay. Clock and transport are injectable; tests use fake time/send only. Queue path is overrideable for tests and defaults under ~/.atlas; never read or print `.env` values.
- Reuse the existing secret redaction behavior before durable queue write; a secret-shaped raw message must not land in queue/receipt.
- Expected existing files: src/atlas/notify.ts, src/atlas/repo-watch.ts, src/atlas/autonomy-loop.ts, src/telegram.ts, src/atlas/commands.ts.
- Expected new focused file if needed: src/atlas/notify-queue.ts. Do not create a second notification authority.
- Tests: extend notify.test.ts, repo-watch.test.ts, autonomy-loop.test.ts. Structural negative: outside notify.ts, production source has zero Telegram API URL/private telegramSend/direct asynchronous sendMessage implementations.

C. SUPERVISED ASSIST — approved-pack fill, external writes fail closed
- Preserve M3 submit guards. Do NOT implement any submit capability. Submit, consent/OAuth, credentials/password/MFA/token, payment, upload/file input and legal acceptance remain adapter-level hard denials forever in this module.
- Live/default mode is read/map/generate-answer-pack. Generic `fillField`/`selectOption` against a non-loopback/non-file origin remains denied.
- Add a versioned ApprovedAnswerPack contract bound to: exact origin, form fingerprint, named field keys/labels, verbatim values, actionPlanHash, SHA-256 pack hash and expiry. The answer pack is generated first, then approved as a whole by the CEO.
- Add an opaque, in-memory, single-session approval capability minted only by a foreground TTY approval port after the human types a challenge containing the pack short-hash. No boolean (`allowPortal`, `allowSubmit`, etc.), planner payload, goal-runner action or serialized file may mint/forge it. Capability is consumed when the assist session starts and cannot be replayed.
- External fill uses a distinct approved-field action referencing answer-pack keys; the adapter resolves the value from the approved pack and inserts it character-for-character. Planner-provided replacement text is ignored/refused. Any pack/file/hash/field/origin/form/actionPlan mismatch or expiry halts before driver mutation.
- In external assist mode intercept network: GET/HEAD may proceed; any unexpected mutating request is aborted, recorded and halts the session. Submit capture guard remains active. Credential-like DOM fields and sensitive autocomplete/input types are denied even if named in a pack.
- Goal-runner must not import the approval port or mint a supervised capability. Real assist is foreground CLI only; headless/non-TTY invocation refuses.
- Add one user command, `atlas assist run <plan> --answers <pack>`, which prints origin/form/field list/hash, requests the exact TTY challenge, launches a visible foreground session only after approval, fills approved fixture fields and leaves final submit to the human. No hidden auto-approve flag.
- Expected existing files: src/hands/browser-actions.ts, src/hands/browser-adapter.ts, src/hands/registry.ts, src/goal-runner/red-line.ts, src/cli.ts.
- Expected new focused files: src/hands/assist-contract.ts, src/hands/assist-approval-port.ts, src/hands/supervised-assist.ts, src/__tests__/supervised-assist.test.ts. Reuse the current local fixture or add one local fixture only; do not contact a real portal.
- FIELD-FILL TIGHTENING (Round 20 Dispute-2 signature): field-fill may only insert values VERBATIM from a CEO-approved answer pack — the pack is generated by Atlas, then explicitly approved by CEO as a document, then filled character-for-character; no on-the-fly rewording of application answers by the planner during a live session. Deviation = halt.

7. DETERMINISTIC ACCEPTANCE / DoD
- Baseline/anti-fork: first commands prove clean tree and HEAD exactly == the spawn-prompt SHA (amendment 2). If not exact, STOP without edits.
- Control round-trip: fixture/temp-state `pause -> goal/task/autonomy blocked -> resume -> allowed`; pause arriving before notify/action causes send/driver call count 0; hard env/file overlays remain effective after control resume.
- Notify chokepoint: repo-watch and async Telegram results use notify.ts; structural scan has no alternate proactive transport. 23:30 Baku important notification => QUEUED + send count 0; panic at 23:30 => SENT; 08:01 drain sends once; failed drain retains queue; chatter remains SUPPRESSED.
- Supervised assist fixture: no TTY/no approval => driver call 0; forged boolean/token/replayed capability/hash mismatch/wrong origin/wrong form/unapproved field/expired pack => driver call 0; approved pack fills every value verbatim; mutating request aborts and halts; submit and credential/upload/payment/consent controls remain denied; goal-runner source cannot import approval port.
- No real portal, live Telegram send, live DB, paid API, deploy, push or merge.
- Targeted tests first, then `npm run typecheck`, `npm run build`, full native Windows `npx vitest run`, full Git Bash `npx vitest run`. Report exact files/pass/fail/skip counts per shell. No "same as previous" shorthand.
- One bounded commit on feat/arsenal-wiring, parent = spawn-prompt SHA, created via sentry-commit discipline. No push. Working tree clean after commit.

Suggested targeted command:
npx vitest run src/__tests__/control-plane.test.ts src/__tests__/spend-policy.test.ts src/__tests__/task-spawner-safety.test.ts src/__tests__/notify.test.ts src/__tests__/repo-watch.test.ts src/__tests__/autonomy-loop.test.ts src/__tests__/browser-hand.test.ts src/__tests__/goal-runner.test.ts src/__tests__/supervised-assist.test.ts

8. NON-GOALS / RED LINES
- No real portal smoke; it is separately CEO-approved and not M7 DoD.
- No submit capability, no credentials/cookies import, no saved browser profile, no consent, no upload, no payment, no legal acceptance.
- No live Telegram test, production deploy, Railway/Supabase/env/DNS/scheduler mutation, paid provider call, push, merge, deletion outside scoped repo or secret read/print.
- No M4 durable memory/instance anti-fork, M6 providers/spend, M5 manifests, M8 audit framework, OPSBOARD edits or new orchestrator framework.
- No `claude update` or any toolchain/OS mutation (token amendment 3).
- Unknown effect/action, unexpected external request, missing approval binding, notification failure, verifier exception or control-state ambiguity => halt/fail closed; never narrative success.

9. RETURN CONTRACT
- No progress narration to CEO.
- Append exactly one final Round to C:\Projects\VOLAURA\memory\atlas\codex-loop.md, BODY terminal-atlas-executor, with six fields: shipped; proof; decisions; deferred; residual risk; commit hash + exact Windows/Git-Bash/build/typecheck counts.
- Explicitly list every UNVERIFIED item (native /goal loop is UNVERIFIED by definition this run). A running process, transcript growth, changed files or self-report without receipts is not DONE.
```
