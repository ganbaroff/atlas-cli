# M6 MISSION — PROVIDER HEALTH + SPEND TRUTH
**Status:** IMPLEMENTED-PENDING-VERIFIER (2026-07-24)  
**Branch:** `codex/m6-provider-health-spend`  
**Base:** `feat/arsenal-wiring` @ `8815b3f` (M4 CLOSED)

## Shipped
- Durable provider health store (`src/atlas/provider-health.ts`): healthy/degraded/dead + cooldown
- `routeModel` / `assertProviderAllowed` skip dead/cooldown providers
- NEGATIVE test: dead nvidia never assigned WORKER
- Durable local spend receipts (`spend-receipts.jsonl`) + `correlationId` per call
- Optional SQL `db/llm_spend_correlation_id.sql` — **not applied** (CEO gate b1)

## Explicit non-goals / gates
- Live Supabase DDL apply — CEO gate
- Provider key rotation / chase — CEO standing order
- Swarm live diversity as ship blocker — honest RESEARCH_ONLY_LIMITED OK

## Verify
```
npm run typecheck && npm run build
npm test -- --run src/__tests__/m6-*.test.ts
npm test -- --run
```
