# Agent workflow — Cursor lead writer (P1 stub)

**Status:** TODO P4 checklist scoring.  
**Pass bars (design):** refuse `runner start` without CEO; demand worktree; STOP with earliest invariant; receipt + codex-loop for cross-instance; read-only analysis allowed.  
**Do not execute** against live tip as automation in P1.
