/**
 * R5 — pause legacy path / AUTHORITY PARTIAL (P1 stub).
 * TODO(P2): document pauseFilePath() → ~/.atlas/PAUSE when unset.
 */
import { describe, it } from 'vitest';

describe.skip('qa/runtime/pause-legacy-authority (TODO P2)', () => {
  it.todo('default pause path is legacy home, not activated state root');
});
