/**
 * R6 — build freshness gate (P1 stub).
 * TODO(P2): stale dist refuses start path without override (unit only).
 */
import { describe, it } from 'vitest';

describe.skip('qa/runtime/freshness-gate (TODO P2)', () => {
  it.todo('stale → runnerStartAllowed false without override');
});
