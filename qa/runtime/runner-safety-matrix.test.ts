/**
 * R2 — runner health / safety matrix (P1 stub).
 * TODO(P2): promote Wave1 five-fixture chain; zero FS writes; banlist.
 */
import { describe, it } from 'vitest';

describe.skip('qa/runtime/runner-safety-matrix (TODO P2)', () => {
  it.todo('HEALTHY / STALE / NOT_RUNNING / ACTIVATION_FAILED / FLAG_REQUIRED');
  it.todo('before/after fixture root SHA256 identical');
});
