/**
 * R7 — injected tick idle (P1 stub).
 * TODO(P2): runnerTick with mock claim→null; no network; no prod deps.
 */
import { describe, it } from 'vitest';

describe.skip('qa/runtime/tick-injected-idle (TODO P2)', () => {
  it.todo('injected claim null → idle status');
});
