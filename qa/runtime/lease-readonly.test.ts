/**
 * R4 — readonly lease open (P1 stub).
 * TODO(P2): getInstanceLeaseInfoReadonly never mkdirSync.
 */
import { describe, it } from 'vitest';

describe.skip('qa/runtime/lease-readonly (TODO P2)', () => {
  it.todo('missing lease dir → null; no directory created');
});
